import { createStore } from 'vuex'

export default createStore({
  state: {
  
  },
  getters: {
  },
  mutations: {
    add(state, LeaveRequests) {
    state.employeeInformation.push(requests);
    }
  
  },
  actions: {
  },
  modules: {
  }
})
